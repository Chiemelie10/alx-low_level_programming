#include <stdio.h>

/**
 * main - Entry point
 *
 * char str[] - Initializing strings
 *
 * puts (str) - Writing to stdout
 *
 * return: Always 0 (Success)
 */
int main()
{
	char str[] = "\"Programming is like building a multilingual puzzle";
	puts(str);
	return (0);
}
